import 'package:flutter/material.dart';

class CSeparator extends StatelessWidget {
  const CSeparator({super.key});

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: 30.0,);
  }
}
